# 软件工程实用规则参考

> 基于 lawsofsoftwareengineering.com 的 56 条经典定律，
> 提炼出非科班开发者最容易用上的核心规则。
> 适用场景：个人项目、小团队协作、学习编程过程中的决策参考。

---

## 一、做决策时

### 1. YAGNI（你不需要它）
**只加当前需要的功能，不为"将来可能用到"提前做。**

- ❌ "以后可能要用到数据库，先装上 PostgreSQL"
- ✅ 先用 JSON 文件存，数据量大了再考虑数据库
- ❌ "这个函数以后可能被复用，先封装成通用接口"
- ✅ 等真的出现第二个调用方再提取

### 2. KISS（保持简单，傻瓜）
**简单的方案永远好过"聪明"的方案。**

- 选你最能理解的技术栈，而不是最新最热门的
- 一个文件能搞定的功能，不要拆成三个文件
- 别人（包括未来的你）读代码时，不需要查文档就能看懂

### 3. 过早优化是万恶之源（Knuth 法则）
**先让功能跑起来，再考虑性能优化。**

- 顺序：**能跑 → 跑对 → 跑快**，不能跳步
- 每次优化前问自己：用户能感知到这个提升吗？
- 如果一段代码只跑一次（比如项目初始化脚本），不必优化

---

## 二、写代码时

### 4. Boy Scout 规则（童子军规则）
**每次离开时，让代码比你发现时更干净。**

- 改一个文件时，顺手把附近没用的注释删了
- 看到一个拼写错误，顺手修了
- 每次提交改动尽量少留"技术债"

### 5. DRY（不要重复自己）
**同样的逻辑只写一次。**

- 如果复制粘贴了同一段代码两次就抽成函数
- 如果两个地方写类似的 HTML 结构就抽成组件
- **注意：** 不要为了"避免重复"过早抽象——两处相同不算 DRY 问题，三处才考虑

### 6. 克尼汉定律
**调试代码比写代码难一倍。如果你的代码写到极限还是看不懂，说明太复杂了。**

- 写代码时花 10 秒钟想想：3 个月后的自己看得懂吗？
- 变量命名比注释更重要——好名字不需要注释
- 函数尽量短，一个函数只做一件事（单一职责）

### 7. 最简惊讶原则
**代码的行为应该符合读者的直觉。**

- 名叫 `save()` 的函数不应该删除数据
- `getUser()` 不应该修改用户信息
- 接口返回格式保持一致：要么全部成功返回 `{ data }`，要么全部 `{ ok }`

---

## 三、管理项目时

### 8. Gall 定律
**能工作的复杂系统，一定是从一个能工作的简单系统演化而来的。**

- 先搭一个能跑的"最小版本"，再逐步加功能
- 永远不要在脑子里"完全设计好"再动手——边做边调整
- 你的第一个版本唯一的目标就是：**能用**

### 9. 帕金森定律
**工作会膨胀到填满分配给它的时间。**

- 不设截止时间的功能，永远做不完
- 给任务设硬性时间盒（比如"1 小时内搞定"）
- "完美"比"完成"更容易浪费时间

### 10. 九十九十规则
**前 90% 的代码花掉 90% 的时间，剩下 10% 的代码再花 90% 的时间。**

- 最后 10% 往往是细节打磨（错误处理、边界情况、文档）
- 做计划时，为"收尾阶段"预留足够时间
- 觉得"快做完了"的时候，实际可能还有一半路要走

---

## 四、与人协作时

### 11. 康威定律
**系统的架构会复制团队的沟通结构。**

- 小团队用简单的架构（单人项目不需要微服务）
- 如果你和同事沟通不畅，代码也会反映出来
- 代码模块的划分应该对应职责的划分

### 12. 布鲁克斯定律
**给一个已经延期的项目加人，只会让它更晚。**

- 个人项目也一样：同时开太多功能，每个都做不完
- **一次只做一个功能**，做完再开下一个
- 优先级排序后，砍掉最不重要的那个

### 13. 汉隆剃刀
**能用"愚蠢"解释的，不要用"恶意"解释。**

- 同事代码写得烂，大概率不是针对你
- 出 bug 了先检查自己的代码，而不是骂别人的接口
- 代码评审时对事不对人

---

## 五、理解系统时

### 14. 特斯拉定律（复杂度守恒）
**每个系统都有不可消除的复杂度，你能做的只是决定它放在哪。**

- 前端简单 → 后端就复杂（反之亦然）
- 手动配置简单 → 自动化的代码就复杂
- 关键是把复杂度放在你/你的团队最能处理的地方

### 15. 抽象漏洞定律
**所有非平凡的抽象层在某个层面上都会泄漏。**

- 用框架省了时间，但框架出问题时你得理解它的底层
- 用 ORM 方便操作数据库，但复杂查询还是要写 SQL
- **理解你的工具栈的下一层**，否则出了问题无从下手

### 16. 波斯特尔定律（鲁棒性原则）
**对自己严格，对别人宽容。**

- 函数输入做校验（对自己严格），但尽量不要因为别人的格式问题崩溃（对别人宽容）
- 你的代码应该能优雅处理意外的输入，而不是直接报 500
- 发送数据时遵守规范，接收数据时容忍轻微偏差

---

## 六、做技术选型时

### 17. 需求稳定性优先于技术先进性
- 选你熟悉的技术 > 选热门的新技术
- 选社区活跃的 > 选个人项目
- 选文档完善的 > 选"看起来很酷"的

### 18. 为删除而设计
**好的架构让删除代码变得容易。**

- 模块之间的依赖越少越好
- 一个模块可以被替换而不影响其他模块
- 如果你不敢删一段代码，说明耦合太严重

---

*来源：https://lawsofsoftwareengineering.com/ — Dr. Milan Milanović*
*整理时间：2026-05-18*
